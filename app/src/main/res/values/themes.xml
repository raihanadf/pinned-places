<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PinnedPlaces" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--         <item name="colorPrimary">@color/red</item>-->
        <item name="colorPrimary">@color/lightRed</item>
    </style>

    <style name="Theme.PinnedPlaces" parent="Base.Theme.PinnedPlaces" />

    <style name="Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_location</item>
        <item name="postSplashScreenTheme">@style/Theme.PinnedPlaces</item>
    </style>

</resources>